
spring:
  thymeleaf:
    cache: true                     # 서버 재시작없이 페이지 렌더링
    enabled: true                   # 타임리프 사용
    prefix: classpath:/templates    # 리소스 경로 설정
    suffix: .html

  security:
    user:
      name: user
      password: 1234
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/todo?serverTimezone=Asia/Seoul
    username: root
    password: 9409

  #data:
  #  web:
  #    pageable:
  #      one-indexed-parameters: true        # paging ?? ?  ? ???? 1? ?? false ? 0 ???.  ?? :  1 -> 0 ?? ?????..? ??? ??? ???? ???
  #      default-page-size: 5                # @PageableDefault ?? ??? ? ???? 10?

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        # show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace


  #  https://steady-coding.tistory.com/601 ?? ?? ??